import { render } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { AppLayout } from './AppLayout'; import { useAuthStore } from '../../../store/authStore'; test('renders dashboard navigation',()=>{ useAuthStore.setState({ user:{ id:1,email:'a',name:'Alice',superAdmin:true,permissions:[] }, accessToken:'x', isAuthenticated:true, isLoading:false }); const view = render(<MemoryRouter><AppLayout title='Home'><div>Body</div></AppLayout></MemoryRouter>); expect(view.getByText('Dashboard')).toBeInTheDocument(); });
