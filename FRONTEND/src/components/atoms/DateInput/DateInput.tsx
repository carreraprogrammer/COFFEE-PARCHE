import styles from './DateInput.module.css';
export interface DateInputProps { name:string; label?:string; value:string; onChange:(value:string)=>void; error?:string; hint?:string; disabled?:boolean; required?:boolean; min?:string; max?:string; format?:'date'|'datetime-local'; }
export const DateInput=({name,label,value,onChange,error,hint,disabled,required,min,max,format='date'}:DateInputProps)=> <div className={styles.wrapper}>{label ? <label htmlFor={name} className={`${styles.label} ${required ? styles.required : ''}`.trim()}>{label}</label> : null}<input id={name} name={name} type={format} className={`${styles.input} ${error ? styles.errorInput : ''}`.trim()} value={value} onChange={(e)=>onChange(e.target.value)} disabled={disabled} required={required} min={min} max={max} />{error ? <p className={styles.error}>{error}</p> : hint ? <p className={styles.hint}>{hint}</p> : null}</div>;
