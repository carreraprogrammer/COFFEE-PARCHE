import styles from './TextInput.module.css';
export interface TextInputProps { name:string; label?:string; placeholder?:string; value:string; onChange:(value:string)=>void; error?:string; hint?:string; disabled?:boolean; required?:boolean; maxLength?:number; autoComplete?:string; }
export const TextInput=({name,label,placeholder,value,onChange,error,hint,disabled,required,maxLength,autoComplete}:TextInputProps)=> <div className={styles.wrapper}>{label ? <label htmlFor={name} className={`${styles.label} ${required ? styles.required : ''}`.trim()}>{label}</label> : null}<input id={name} name={name} className={`${styles.input} ${error ? styles.errorInput : ''}`.trim()} placeholder={placeholder} value={value} onChange={(e)=>onChange(e.target.value)} disabled={disabled} required={required} maxLength={maxLength} autoComplete={autoComplete} />{error ? <p className={styles.error}>{error}</p> : hint ? <p className={styles.hint}>{hint}</p> : null}</div>;
