import { render } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { Header } from './Header'; import { useAuthStore } from '../../../store/authStore'; test('renders current user name',()=>{ useAuthStore.setState({ user:{ id:1,email:'a',name:'Alice',superAdmin:false,permissions:[] }, accessToken:'x', isAuthenticated:true, isLoading:false }); const view = render(<MemoryRouter><Header /></MemoryRouter>); expect(view.getByText('Alice')).toBeInTheDocument(); });
