import { useEffect, useState } from 'react';
import { Button } from '../../atoms/Button';
import { DynamicField } from '../../molecules/DynamicField';
import { formService } from '../../../services/formService';
import type { ApiEnvelope, FormSchema, FormValues } from '../../../types/form.types';
import styles from './DynamicForm.module.css';
const buildValues=(schema:FormSchema,initialValues:FormValues):FormValues=> Object.fromEntries((schema.fields ?? []).map((field)=>[field.name, initialValues[field.name] ?? (field.type==='checkbox' ? false : '')]));
export const DynamicForm=({schema,initialValues={},onSuccess,onError,readOnly=false}:{schema:FormSchema;initialValues?:FormValues;onSuccess?:(payload:ApiEnvelope<Record<string, unknown>>)=>void;onError?:(error:unknown)=>void;readOnly?:boolean;})=>{ const [values,setValues]=useState<FormValues>(()=>buildValues(schema,initialValues)); const [errors]=useState<Record<string,string>>({}); const [isSubmitting,setIsSubmitting]=useState(false); const fields=schema.fields ?? []; useEffect(()=>{ setValues(buildValues(schema,initialValues)); },[schema.slug]); return <form className={styles.form} noValidate onSubmit={async (e)=>{ e.preventDefault(); e.stopPropagation(); if(readOnly) return; console.log('[DynamicForm] submit:start',{ slug:schema.slug, endpoint:schema.submit_endpoint, method:schema.submit_method, values }); setIsSubmitting(true); try { const response=await formService.submit(schema,values); console.log('[DynamicForm] submit:success',response); onSuccess?.(response); } catch (error) { console.error('[DynamicForm] submit:error',error); onError?.(error); } finally { setIsSubmitting(false); } }}>{fields.map((field)=><DynamicField key={field.name} field={field} value={values[field.name]} onChange={(value)=>setValues((current)=>({ ...current, [field.name]: value }))} error={errors[field.name]} />)}{!readOnly ? <Button type='submit' label='Enviar' loading={isSubmitting} fullWidth /> : null}</form>; };
