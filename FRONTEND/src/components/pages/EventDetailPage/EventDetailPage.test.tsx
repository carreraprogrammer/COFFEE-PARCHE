import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { EventDetailPage } from './EventDetailPage'; describe('EventDetailPage',()=>{ it('renders',()=>{ render(<MemoryRouter><EventDetailPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Detalle del parche' })).toBeInTheDocument(); }); });
