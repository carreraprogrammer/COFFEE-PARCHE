import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { ParticipantsPage } from './ParticipantsPage'; describe('ParticipantsPage',()=>{ it('renders',()=>{ render(<MemoryRouter><ParticipantsPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Participantes' })).toBeInTheDocument(); }); });
