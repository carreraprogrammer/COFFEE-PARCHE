import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { EnrollmentsPage } from './EnrollmentsPage'; describe('EnrollmentsPage',()=>{ it('renders',()=>{ render(<MemoryRouter><EnrollmentsPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Inscripciones' })).toBeInTheDocument(); }); });
