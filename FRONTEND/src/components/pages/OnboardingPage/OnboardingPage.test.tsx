import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { OnboardingPage } from './OnboardingPage'; describe('OnboardingPage',()=>{ it('renders first step',()=>{ render(<MemoryRouter><OnboardingPage /></MemoryRouter>); expect(screen.getByText(/Cuéntanos quién eres/i)).toBeInTheDocument(); }); });
