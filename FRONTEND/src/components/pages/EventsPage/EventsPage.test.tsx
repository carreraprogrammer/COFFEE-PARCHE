import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it, vi } from 'vitest'; vi.mock('../../../services/eventService',()=>({ eventService:{ getAll: vi.fn().mockResolvedValue([]) } })); import { EventsPage } from './EventsPage'; describe('EventsPage',()=>{ it('renders layout title', async ()=>{ render(<MemoryRouter><EventsPage /></MemoryRouter>); expect(await screen.findByRole('heading', { name: /¿Listo para tu próximo parche/i })).toBeInTheDocument(); }); });
