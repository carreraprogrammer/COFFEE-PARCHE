import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { PartnersPage } from './PartnersPage'; describe('PartnersPage',()=>{ it('renders',()=>{ render(<MemoryRouter><PartnersPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Aliados' })).toBeInTheDocument(); }); });
