import { render } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { ProfilePage } from './ProfilePage'; import { useAuthStore } from '../../../store/authStore'; test('renders profile title',()=>{ useAuthStore.setState({ user:{ id:1,email:'a',name:'Alice',superAdmin:false,permissions:[] }, accessToken:'x', isAuthenticated:true, isLoading:false }); const view = render(<MemoryRouter><ProfilePage /></MemoryRouter>); expect(view.getByRole('heading', { name:'Mi perfil' })).toBeInTheDocument(); });
