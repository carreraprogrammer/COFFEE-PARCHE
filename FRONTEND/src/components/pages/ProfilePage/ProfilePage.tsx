import { AppLayout } from '../../templates/AppLayout'; import { DynamicForm } from '../../organisms/DynamicForm'; import { useAuthStore } from '../../../store/authStore'; import styles from './ProfilePage.module.css';
export const ProfilePage=()=>{ const user=useAuthStore((state)=>state.user); return <AppLayout title='Mi perfil'><section className={styles.grid}><article className={styles.summaryCard}><span className={styles.eyebrow}>Perfil</span><h2 className={styles.title}>Gestiona tu información</h2><p className={styles.description}>Mantén tus datos actualizados y revisa rápidamente el alcance actual de tu cuenta dentro del workspace.</p><div className={styles.userMeta}><div><span className={styles.metaLabel}>Nombre</span><strong>{user?.name ?? 'Sin nombre'}</strong></div><div><span className={styles.metaLabel}>Correo</span><strong>{user?.email ?? 'Sin correo'}</strong></div><div><span className={styles.metaLabel}>Rol</span><strong>{user?.superAdmin ? 'Superadministrador' : 'Colaborador'}</strong></div></div></article><article className={styles.formCard}><DynamicForm schema={{ slug:'profile-form', title:'Perfil', submit_endpoint:`/api/v1/users/${user?.id ?? ':id'}`, submit_method:'PATCH', fields:[{ name:'name', type:'text', label:'Nombre' },{ name:'email', type:'email', label:'Email' }] }} initialValues={{ name:user?.name ?? '', email:user?.email ?? '' }} /></article></section></AppLayout>; };
