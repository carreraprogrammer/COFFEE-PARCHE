import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { AdminEventsPage } from './AdminEventsPage'; describe('AdminEventsPage',()=>{ it('renders',()=>{ render(<MemoryRouter><AdminEventsPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Gestionar eventos' })).toBeInTheDocument(); }); });
