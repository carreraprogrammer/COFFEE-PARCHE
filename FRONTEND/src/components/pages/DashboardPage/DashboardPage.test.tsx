import { render } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { DashboardPage } from './DashboardPage'; import { useAuthStore } from '../../../store/authStore'; test('renders greeting',()=>{ useAuthStore.setState({ user:{ id:1,email:'a',name:'Alice',superAdmin:false,permissions:[] }, accessToken:'x', isAuthenticated:true, isLoading:false }); const view = render(<MemoryRouter><DashboardPage /></MemoryRouter>); expect(view.getByText('Hola, Alice')).toBeInTheDocument(); });
