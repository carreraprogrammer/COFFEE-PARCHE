import { render } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { LoginPage } from './LoginPage'; import { useFormStore } from '../../../store/formStore'; test('renders register link',()=>{ useFormStore.setState({ schemas:{ 'login-form': { slug:'login-form', title:'Login', submit_endpoint:'/api/v1/auth/login', submit_method:'POST', fields:[{ name:'email', type:'email', label:'Email' }] } }, isLoading:false, error:null }); const view = render(<MemoryRouter><LoginPage /></MemoryRouter>); expect(view.getByText('¿No tienes cuenta? Regístrate')).toBeInTheDocument(); });
