import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { AdminEventDetailPage } from './AdminEventDetailPage'; describe('AdminEventDetailPage',()=>{ it('renders',()=>{ render(<MemoryRouter><AdminEventDetailPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Detalle de evento' })).toBeInTheDocument(); }); });
