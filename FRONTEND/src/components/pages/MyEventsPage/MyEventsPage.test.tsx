import { MemoryRouter } from 'react-router-dom'; import { render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import { MyEventsPage } from './MyEventsPage'; describe('MyEventsPage',()=>{ it('renders',()=>{ render(<MemoryRouter><MyEventsPage /></MemoryRouter>); expect(screen.getByRole('heading', { name: 'Mis parches' })).toBeInTheDocument(); }); });
